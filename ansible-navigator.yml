---
ansible-navigator:
  ansible:
    config: ./ansible.cfg

  execution-environment:
    #image: ee-supported-rhel8:2.0
    # Log in first with: podman login registry.redhat.io
    image: registry.redhat.io/ansible-automation-platform-21/ee-supported-rhel8:1.0
    pull-policy: missing

  playbook-artifact:
    enable: False
